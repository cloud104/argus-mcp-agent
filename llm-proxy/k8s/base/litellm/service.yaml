apiVersion: v1
kind: Service
metadata:
  name: litellm-proxy-service
  namespace: argus
spec:
  selector:
    app: litellm-proxy
  ports:
  - protocol: TCP
    port: 80
    targetPort: 4000
  type: ClusterIP
